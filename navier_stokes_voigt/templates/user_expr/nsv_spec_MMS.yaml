scale: 5.0

# psi initial guess
numpy_psi0: "np.sin(2*pi*x / L)*np.sin(2*pi*y / H)*np.exp(-t)"
numpy_psi_t: "-np.sin(2*pi*x / L)*np.sin(2*pi*y / H)*np.exp(-t)"

# source term
numpy_f: "(-1 + (-alpha**2 + 1/Re)*( (2*pi/L)**2 + (2*pi/H)**2 ))*( np.sin(2*pi*x / L)*np.sin(2*pi*y / H)*np.exp(-t) )"
