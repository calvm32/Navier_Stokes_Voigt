# velocity exact
ufl_v0: "as_vector([
    Re*(sin(y*pi/H)*exp((-1*pi**2*t)/(H**2*Re)) + 0.5*P*y*(y - H)),
    0.0
  ])"

ufl_inflow: "ufl_v0"

# pressure exact
ufl_p0: "(P*x + G)"

# source term exact
ufl_f: "as_vector([0.0,0.0])"