# velocity exact
ufl_v0: "as_vector([
    Re*(sin(y*pi/H)*exp((-1*pi**2*t)/(H**2*Re)) + 0.5*P*y*(y - H)),
    0.0
  ])"

# pressure exact
ufl_p0: "(P*x + G)"

# source term exact
ufl_f: "as_vector([-1*((alpha**2)*(pi**4) / (H**4))* sin(y*pi/H) *exp((-1*pi**2*t)/(H**2*Re)), 0.0])"