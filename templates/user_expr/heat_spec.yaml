# exact functions for u=e^t*sin(pix)*cos(piy)

# initial condition u0 
numpy_u0: "exp(t)*cos(pi*x)*cos(pi*y)"  

# source term f 
numpy_f: "(1+2*pi**2)*exp(t)*cos(pi*x)*cos(pi*y)" 

# bdy condition g
numpy_g: "0.0"  