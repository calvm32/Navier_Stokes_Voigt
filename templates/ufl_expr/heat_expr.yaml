# exact functions for u=e^t*sin(pix)*cos(piy)
ufl_u0: "exp(t)*cos(pi*x)*cos(pi*y)"               # initial condition u0 

ufl_f: "(1+2*pi**2)*exp(t)*cos(pi*x)*cos(pi*y)"    # source term f 
ufl_g: "Constant(0)"                               # bdy condition g