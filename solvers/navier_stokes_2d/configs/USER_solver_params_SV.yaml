solver_parameters:
  mat_type: aij

  # Solve linearized system only
  snes_type: ksponly

  # Krylov solver
  ksp_type: fgmres
  ksp_rtol: 1e-6
  ksp_atol: 1e-12
  ksp_max_it: 500

  # Block Schur
  pc_type: lu
